header.header
    .header__wrapper
        .header__column
            a(href=routes.home)
                i.fas.fa-tools
        .header__column
            ul
                if !loggedUser
                    li
                        a(href=routes.join)
                            span JOIN
                    li
                        a(href=routes.login)
                         span LOG IN
                else
                    li
                        a(href=routes.me)
                            span=loggedUser.name
                    li
                        a(href=routes.logout)
                            span LOG OUT
